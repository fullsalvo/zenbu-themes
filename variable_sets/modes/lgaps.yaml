inner:                "20"
outer:                "50"

dunsthgap:            "{{ ((inner | float)/7 + (outer | float)/7) | int }}"
dunstvgap:            "{{ ((inner | float)/7 + (outer | float)/7 + (barheight | float)) | int }}"
